development:
  adapter: async

test:
  adapter: test

production:
  # No real-time features exist anywhere in the app (app/channels is unused
  # rails-new boilerplate), so there's nothing that needs cross-process
  # broadcasting — async (in-process, same as dev) avoids provisioning Redis
  # in production for a capability nothing actually uses.
  adapter: async
