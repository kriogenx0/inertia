<h1>Tasks</h1>

<div id="tasks">
  <% @tasks.each do |task| %>
    <div>
      <%= link_to task.name.presence || "Untitled task", task %>
      <span><%= task.status %></span>
    </div>
  <% end %>
</div>

<%= link_to "New task", new_task_path %>
